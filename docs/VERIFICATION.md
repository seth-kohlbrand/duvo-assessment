# Run every item. Check nothing that wasn't actually executed.

- [ ] Full happy-path flow exercised against real inputs end to end
- [ ] Edge cases exercised (invalid input rejected, duplicate request creates no duplicate side effects)
- [ ] `pytest` passes
- [ ] `docker build` succeeds from clean state
- [ ] `docker run` serves on the documented port
- [ ] Every command in `DEPLOYMENT.md` run verbatim in order
- [ ] No secrets in repo and Git log clean
- [ ] All documentation claims audited against what was actually built
